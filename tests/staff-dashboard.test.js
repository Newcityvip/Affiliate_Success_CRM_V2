const assert=require('assert'),fs=require('fs'),path=require('path'),vm=require('vm');const root=path.resolve(__dirname,'..'),code=fs.readFileSync(path.join(root,'backend','StaffDashboard.gs'),'utf8'),page=fs.readFileSync(path.join(root,'app','dashboard','page.tsx'),'utf8');assert(code.includes("requireRole_(u,['STAFF'])"),'staff dashboard must be STAFF-only');assert(code.includes('myWork_(u'));assert(code.includes('myFollowups_(u'));assert(code.includes('tasks_(u'));assert(code.includes('affiliateDirectory_(u'));assert(!code.includes('superAdminDashboard_'));assert(page.includes('getStaffDashboard'));assert(page.includes('Needs My Attention'));new vm.Script(code);console.log('staff dashboard tests passed');
