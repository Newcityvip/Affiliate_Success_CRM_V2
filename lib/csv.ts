export function csvText(headers:string[],rows:Array<Array<unknown>>){const escape=(value:unknown)=>{let text=String(value??'').replace(/\r?\n/g,'\r\n');if(/^[=+\-@]/.test(text))text="'"+text;return`"${text.replace(/"/g,'""')}"`};return '\uFEFF'+[headers,...rows].map(row=>row.map(escape).join(',')).join('\r\n')}
export function downloadCsv(filename:string,headers:string[],rows:Array<Array<unknown>>){const blob=new Blob([csvText(headers,rows)],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=filename;document.body.appendChild(link);link.click();link.remove();URL.revokeObjectURL(url)}
