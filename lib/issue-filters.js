function filterIssues(items,filters){var q=String(filters.search||'').trim().toLowerCase();return items.filter(function(x){if(filters.card&&filters.card!=='all'){if(filters.card==='URGENT'){if(x.priority!=='URGENT'||x.status==='CLOSED')return false}else if(x.status!==filters.card)return false}if(filters.status&&x.status!==filters.status)return false;if(filters.priority&&x.priority!==filters.priority)return false;if(filters.issueType&&x.issueType!==filters.issueType)return false;if(filters.brand&&x.brandId!==filters.brand)return false;if(filters.staffId&&x.assignedToId!==filters.staffId&&x.reportedById!==filters.staffId)return false;if(q&&!`${x.issueId} ${x.title} ${x.description} ${x.affiliateUsername} ${x.affiliateName} ${x.brandName} ${x.brandCode} ${x.issueType} ${x.reportedByName} ${x.assignedToName} ${x.resolution}`.toLowerCase().includes(q))return false;return true})}
function toggleIssueCard(current,next){return current===next?'all':next}
module.exports={filterIssues,toggleIssueCard};
