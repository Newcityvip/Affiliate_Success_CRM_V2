function filterAffiliates(items,filter,search){const query=String(search||'').trim().toLowerCase();return items.filter(a=>filter==='all'||filter==='telegram'&&(a.telegramStatus==='CONNECTED'||a.lifecycleStatus==='TELEGRAM_CONNECTED')||filter==='prospects'&&['NEW','ACTIVE'].includes(a.prospectStatus)&&a.archiveStatus!=='ARCHIVED'||filter==='work'&&a.activeWorkCount>0).filter(a=>!query||[a.affiliateUsername,a.affiliateName,a.email,a.phone,a.telegramUsername,a.brandName,a.brandCode,a.lifecycleStatus,a.telegramStatus].some(value=>String(value||'').toLowerCase().includes(query)))}
module.exports={filterAffiliates};
