'use client';
import Link from 'next/link';import{useCallback,useEffect,useMemo,useState}from'react';import{api,ApiError,TaskItem,TasksResponse}from'../../lib/api-client';import{clearReadCache,readCache,writeCache}from'../../lib/read-cache';import{filterTasks,TaskCard,TaskFilters,toggleTaskCard}from'../../lib/task-filters';import styles from'./tasks.module.css';
const EMPTY:TaskFilters={card:'open',search:'',staffId:'',brand:'',priority:'',status:''};
export default function TasksPage(){const[initial]=useState(()=>readCache<TasksResponse>('tasks')),[data,setData]=useState<TasksResponse|null>(initial),[loading,setLoading]=useState(!initial),[error,setError]=useState(''),[filters,setFilters]=useState<TaskFilters>(EMPTY),[creating,setCreating]=useState(false);const load=useCallback(async()=>{setError('');try{const r=await api.getTasks();setData(r);writeCache('tasks',r)}catch(c){const auth=c instanceof ApiError&&['UNAUTHENTICATED','SESSION_EXPIRED','FORBIDDEN'].includes(c.code);if(!initial||auth){if(auth){clearReadCache();setData(null)}setError(c instanceof ApiError?c.message:'Tasks could not be loaded.')}}finally{setLoading(false)}},[initial]);useEffect(()=>{void load()},[load]);const filtered=useMemo(()=>filterTasks(data?.items||[],filters),[data,filters]);const cards:Array<[string,number,TaskCard]>=data?[['Open Tasks',data.summary.open,'open'],['Overdue',data.summary.overdue,'overdue'],['Due Today',data.summary.dueToday,'today'],['High Priority',data.summary.high,'high'],['Completed',data.summary.completed,'completed']]:[];return <><div className="section-head"><div><h1>Tasks</h1><p className="muted">Manual operational assignments, separate from system-generated My Work.</p></div><div className={styles.head}>{data?.canManage&&<button className="primary" onClick={()=>setCreating(v=>!v)}>+ Create Task</button>}<button onClick={()=>void load()}>Refresh</button></div></div>{creating&&data&&<CreateTask data={data} onDone={async()=>{setCreating(false);await load()}} onCancel={()=>setCreating(false)}/>} {data&&<><div className={styles.summary}>{cards.map(([label,n,key])=><button className={`card ${styles.summaryButton} ${filters.card===key?styles.active:''}`} aria-pressed={filters.card===key} key={key} onClick={()=>setFilters(f=>({...f,card:toggleTaskCard(f.card,key)}))}><span>{label}</span><b>{n}</b></button>)}</div><Filters data={data} value={filters} onChange={setFilters}/></>}{loading?<State title="Loading tasks…" detail="Retrieving the authorized task workspace."/>:error?<State title="Unable to load Tasks" detail={error} action={<button className="primary" onClick={()=>void load()}>Retry</button>}/>:!data?.items.length?<State title="No tasks yet" detail="No task history exists in your authorized scope."/>:!filtered.length?<State title="No matching tasks" detail="Adjust or clear the selected filters." action={<button onClick={()=>setFilters(EMPTY)}>Clear filters</button>}/>:<TaskList items={filtered} data={data} onChanged={load}/>}</>}
function Filters({data,value,onChange}:{data:TasksResponse;value:TaskFilters;onChange:(v:TaskFilters)=>void}){const brands=Array.from(new Map(data.items.filter(x=>x.brandId).map(x=>[x.brandId,x.brandName||x.brandCode])).entries());return <div className={styles.filters}><input value={value.search} onChange={e=>onChange({...value,search:e.target.value})} placeholder="Search task, affiliate, owner or notes"/>{data.canManage&&<select value={value.staffId} onChange={e=>onChange({...value,staffId:e.target.value})}><option value="">All staff</option>{data.options.staff.map(s=><option value={s.staffId} key={s.staffId}>{s.displayName}</option>)}</select>}<select value={value.brand} onChange={e=>onChange({...value,brand:e.target.value})}><option value="">All brands</option>{brands.map(([id,name])=><option value={id} key={id}>{name}</option>)}</select><select value={value.priority} onChange={e=>onChange({...value,priority:e.target.value})}><option value="">All priorities</option><option>HIGH</option><option>NORMAL</option><option>LOW</option></select><select value={value.status} onChange={e=>onChange({...value,status:e.target.value})}><option value="">All statuses</option><option>PENDING</option><option>IN_PROGRESS</option><option>COMPLETED</option><option>CANCELLED</option></select><button onClick={()=>onChange(EMPTY)}>Clear</button></div>}
function TaskList({items,data,onChanged}:{items:TaskItem[];data:TasksResponse;onChanged:()=>Promise<void>}){return <section className="card"><div className="section-head"><div><h2>Task schedule</h2><p className="muted">{items.length} record{items.length===1?'':'s'}</p></div></div><div className={styles.tableWrap}><table className="table"><thead><tr><th>Affiliate</th><th>Task</th><th>Priority</th><th>Status</th><th>Due</th><th>Owner</th><th>Actions</th></tr></thead><tbody>{items.map(x=><tr className={x.overdue?styles.overdue:''} key={x.taskId}><td><strong>{x.affiliateUsername||'General task'}</strong><small>{x.affiliateName||x.brandName||'Not affiliate-linked'}</small>{x.affiliateUsername&&<Copy value={x.affiliateUsername}/>}</td><td><strong>{x.title}</strong><small>{label_(x.taskType)} · {x.description||x.taskId}</small></td><td>{label_(x.priority)}</td><td><Badge item={x}/></td><td>{date_(x.dueAt)}</td><td>{x.ownerName}</td><td><Actions item={x} data={data} onChanged={onChanged}/></td></tr>)}</tbody></table></div><div className={styles.mobile}>{items.map(x=><article key={x.taskId}><strong>{x.title}</strong><span>{x.affiliateUsername||'General task'} · {x.ownerName}</span><p>{label_(x.taskType)} · {label_(x.priority)} · {date_(x.dueAt)}</p><Badge item={x}/><Actions item={x} data={data} onChanged={onChanged}/></article>)}</div></section>}
function Actions({item,data,onChanged}:{item:TaskItem;data:TasksResponse;onChanged:()=>Promise<void>}){const[busy,setBusy]=useState(false),[note,setNote]=useState(''),[owner,setOwner]=useState(item.staffId),open=['PENDING','IN_PROGRESS'].includes(item.status);async function act(run:()=>Promise<unknown>){if(busy)return;setBusy(true);try{await run();await onChanged()}finally{setBusy(false)}}return <div className={styles.actions}>{item.affiliateId&&<Link href={`/affiliates?affiliateId=${encodeURIComponent(item.affiliateId)}`}>View Affiliate</Link>}{item.status==='PENDING'&&<button disabled={busy} onClick={()=>void act(()=>api.startTask(item.taskId))}>Start</button>}{open&&<><input aria-label="Completion note" value={note} onChange={e=>setNote(e.target.value)} placeholder="Completion note"/><button disabled={busy} onClick={()=>void act(()=>api.completeTask(item.taskId,note))}>Complete</button></>}{data.canManage&&open&&<><select value={owner} onChange={e=>setOwner(e.target.value)}>{data.options.staff.map(s=><option key={s.staffId} value={s.staffId}>{s.displayName}</option>)}</select>{owner!==item.staffId&&<button disabled={busy} onClick={()=>void act(()=>api.reassignTask(item.taskId,owner))}>Reassign</button>}<button disabled={busy} onClick={()=>void act(()=>api.cancelTask(item.taskId))}>Cancel</button></>}</div>}
function CreateTask({data,onDone,onCancel}:{data:TasksResponse;onDone:()=>Promise<void>;onCancel:()=>void}){const[staffId,setStaffId]=useState(data.options.staff[0]?.staffId||''),[affiliateId,setAffiliateId]=useState(''),[taskType,setTaskType]=useState('FOLLOW_UP'),[title,setTitle]=useState(''),[description,setDescription]=useState(''),[priority,setPriority]=useState('NORMAL'),[dueAt,setDueAt]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const affiliates=data.options.affiliates.filter(a=>!staffId||a.staffId===staffId),selected=data.options.affiliates.find(a=>a.affiliateId===affiliateId);async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);setError('');try{await api.createTask({staffId,affiliateId,taskType,title,description,priority,dueAt:dueAt?new Date(dueAt).toISOString():'',clientRequestId:crypto.randomUUID()});await onDone()}catch(c){setError(c instanceof ApiError?c.message:'Task could not be created.');setBusy(false)}}return <form className={`card ${styles.create}`} onSubmit={submit}><div className="section-head"><div><h2>Create operational task</h2><p className="muted">Affiliate-linked tasks must follow the current active assignment.</p></div><button type="button" onClick={onCancel}>Close</button></div>{error&&<p className="error-message">{error}</p>}<div className={styles.formGrid}><label>Assigned Staff *<select required value={staffId} onChange={e=>{setStaffId(e.target.value);setAffiliateId('')}}>{data.options.staff.map(s=><option value={s.staffId} key={s.staffId}>{s.displayName}</option>)}</select></label><label>Affiliate (optional)<select value={affiliateId} onChange={e=>setAffiliateId(e.target.value)}><option value="">General task</option>{affiliates.map(a=><option value={a.affiliateId} key={a.affiliateId}>{a.affiliateUsername} · {a.brandName}</option>)}</select></label><label>Task Type *<input required value={taskType} onChange={e=>setTaskType(e.target.value)}/></label><label>Title *<input required maxLength={160} value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Priority<select value={priority} onChange={e=>setPriority(e.target.value)}><option>HIGH</option><option>NORMAL</option><option>LOW</option></select></label><label>Due date/time<input type="datetime-local" value={dueAt} onChange={e=>setDueAt(e.target.value)}/></label><label className={styles.wide}>Instructions / Notes<textarea maxLength={2000} value={description} onChange={e=>setDescription(e.target.value)}/></label></div>{selected&&<p className={styles.context}><b>{selected.affiliateUsername}</b> · {selected.brandName} · {selected.staffName} · {label_(selected.lifecycleStatus)}</p>}<button className="primary" disabled={busy||!staffId}>{busy?'Creating…':'Create Task'}</button></form>}
function Copy({value}:{value:string}){const[copied,setCopied]=useState(false);return <button className={styles.copy} onClick={async()=>{await navigator.clipboard.writeText(value);setCopied(true);setTimeout(()=>setCopied(false),1200)}}>{copied?'Copied':'Copy Username'}</button>}function Badge({item}:{item:TaskItem}){return <span className={`${styles.badge} ${item.overdue?styles.urgent:''}`}>{label_(item.overdue?'OVERDUE':item.status)}</span>}function State({title,detail,action}:{title:string;detail:string;action?:React.ReactNode}){return <section className="card empty-state"><div><div className="empty-icon">□</div><h2>{title}</h2><p className="muted">{detail}</p>{action}</div></section>}function label_(v:string){return String(v||'—').toLowerCase().replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase())}function date_(v:string){if(!v)return'No due date';const d=new Date(v);return Number.isNaN(d.getTime())?'Invalid date':new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(d)}
