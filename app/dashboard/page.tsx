export default function DashboardPage(){return <><h1>Command Center</h1><p className="muted">Your operational overview will appear as live dashboard endpoints are connected.</p><section className="grid" style={{marginTop:20}}>{['Open actions','Overdue','Telegram connected','Reactivated'].map((label)=><div className="card" key={label}><div className="card-label">{label}</div><div className="kpi">—</div><span className="muted">No live metric available</span></div>)}</section><section className="card empty-state" style={{marginTop:20}}><div><div className="empty-icon">◫</div><h2>No command-center data yet</h2><p className="muted">The authenticated production shell is ready. Operational metrics remain empty until a scoped dashboard endpoint is implemented.</p></div></section></>}
