function dashboardOpen_(status){return ['PENDING','IN_PROGRESS','OVERDUE'].indexOf(String(status))>=0}
function dashboardTime_(value){var ms=value?new Date(value).getTime():NaN;return isFinite(ms)?ms:NaN}
function dashboardToday_(ms,now){if(!isFinite(ms))return false;var a=new Date(ms),b=new Date(now);return a.getFullYear()===b.getFullYear()&&a.getMonth()===b.getMonth()&&a.getDate()===b.getDate()}
function dashboardPipeline_(affiliate){var lifecycle=String(affiliate.Lifecycle_Status||''),prospect=String(affiliate.Prospect_Status||''),telegram=String(affiliate.Telegram_Status||'');if(affiliate.Archive_Status==='ARCHIVED'||lifecycle==='ARCHIVED'||prospect==='CLOSED')return 'CLOSED';if(telegram==='CONNECTED'||lifecycle==='TELEGRAM_CONNECTED')return 'TELEGRAM_CONNECTED';if(lifecycle==='CONNECTED')return 'CONNECTED';if(lifecycle==='CALLBACK'||lifecycle==='RETRY')return 'CALLBACK_RETRY';if(lifecycle==='CONTACTING')return 'CONTACTING';return 'NEW_ASSIGNED'}
function superAdminDashboard_(user,p){
  requireRole_(user,['SUPER_ADMIN']);var now=Date.now(),periodStart=now-30*86400000,affiliates=rows_('Affiliates'),assignments=rows_('Assignments'),staffRows=rows_('Staff_List'),teams=rows_('Team_List'),brands=rows_('Brand_List'),works=rows_('Work_Items'),followups=rows_('Followups'),interactions=rows_('Interactions'),attempts=rows_('Contact_Attempts'),affiliateById={},staffById={},teamById={},brandById={},activeAssignment={},workByAffiliate={},followupByAffiliate={},lastEvent={},interactionWork={},staffMetrics={};
  affiliates.forEach(function(a){affiliateById[String(a.Affiliate_ID)]=a});teams.forEach(function(t){teamById[String(t.Team_ID)]=t});brands.forEach(function(b){brandById[String(b.Brand_ID)]=b});staffRows.forEach(function(s){staffById[String(s.Staff_ID)]=s;if(s.Role==='STAFF'&&s.Status==='ACTIVE')staffMetrics[String(s.Staff_ID)]={staffId:s.Staff_ID,displayName:s.Display_Name||s.Username||'',username:s.Username||'',team:(teamById[String(s.Team)]||{}).Team_Name||s.Team||'',activeAffiliates:0,openWork:0,overdueWork:0,contactsToday:0,interactions30d:0,connectedAffiliates:0,telegramConnected:0,openFollowups:0,overdueFollowups:0,connectionRate:0}});
  assignments.forEach(function(a){var key=String(a.Affiliate_ID),affiliate=affiliateById[key],staff=staffById[String(a.Staff_ID)];if(a.Status!=='ACTIVE'||!affiliate||!staff||staff.Status!=='ACTIVE'||activeAssignment[key])return;activeAssignment[key]=a});Object.keys(activeAssignment).forEach(function(key){var a=activeAssignment[key],affiliate=affiliateById[key],metrics=staffMetrics[String(a.Staff_ID)];if(metrics){metrics.activeAffiliates++;var stage=dashboardPipeline_(affiliate);if(stage==='CONNECTED'||stage==='TELEGRAM_CONNECTED')metrics.connectedAffiliates++;if(stage==='TELEGRAM_CONNECTED')metrics.telegramConnected++}});
  works.forEach(function(w){var assignment=activeAssignment[String(w.Affiliate_ID)];if(!assignment||String(assignment.Assignment_ID)!==String(w.Assignment_ID)||String(assignment.Staff_ID)!==String(w.Staff_ID)||!dashboardOpen_(w.Status))return;var key=String(w.Affiliate_ID),list=workByAffiliate[key]||(workByAffiliate[key]=[]),due=dashboardTime_(w.Due_At),overdue=isFinite(due)&&due<now;list.push({workId:w.Work_ID,type:w.Work_Type||'',status:w.Status||'',dueAt:w.Due_At||'',overdue:overdue});var metrics=staffMetrics[String(w.Staff_ID)];if(metrics){metrics.openWork++;if(overdue)metrics.overdueWork++}});
  followups.forEach(function(f){var assignment=activeAssignment[String(f.Affiliate_ID)];if(!assignment||String(assignment.Assignment_ID)!==String(f.Assignment_ID)||String(assignment.Staff_ID)!==String(f.Staff_ID)||!dashboardOpen_(f.Status))return;var key=String(f.Affiliate_ID),list=followupByAffiliate[key]||(followupByAffiliate[key]=[]),due=dashboardTime_(f.Due_At),overdue=isFinite(due)&&due<now;list.push({followupId:f.Followup_ID,type:f.Followup_Type||'',dueAt:f.Due_At||'',overdue:overdue});var metrics=staffMetrics[String(f.Staff_ID)];if(metrics){metrics.openFollowups++;if(overdue)metrics.overdueFollowups++}});
  interactions.forEach(function(i){if(i.Work_ID)interactionWork[String(i.Work_ID)]=true});function countEvent(row,idField,timeField,type,outcome){var assignment=activeAssignment[String(row.Affiliate_ID)];if(!assignment||String(assignment.Staff_ID)!==String(row.Staff_ID))return;var time=dashboardTime_(row[timeField]||row.Created_At),metrics=staffMetrics[String(row.Staff_ID)];if(metrics){if(dashboardToday_(time,now))metrics.contactsToday++;if(isFinite(time)&&time>=periodStart)metrics.interactions30d++}var key=String(row.Affiliate_ID),current=lastEvent[key];if(!current||time>current.time)lastEvent[key]={time:time,timestamp:row[timeField]||row.Created_At||'',outcome:row[outcome]||'',type:type,id:row[idField]||''}}
  interactions.forEach(function(i){countEvent(i,'Interaction_ID','Interaction_At',i.Interaction_Type||'INTERACTION','Outcome')});attempts.forEach(function(a){if(a.Work_ID&&interactionWork[String(a.Work_ID)])return;countEvent(a,'Attempt_ID','Attempt_At','CONTACT_ATTEMPT','Result')});
  var activeAffiliates=0,activeProspects=0,connected=0,telegramConnected=0,pipeline={NEW_ASSIGNED:0,CONTACTING:0,CALLBACK_RETRY:0,CONNECTED:0,TELEGRAM_CONNECTED:0,CLOSED:0},affiliateItems=[];affiliates.forEach(function(a){var archived=a.Archive_Status==='ARCHIVED'||a.Lifecycle_Status==='ARCHIVED',key=String(a.Affiliate_ID),assignment=activeAssignment[key]||{},actor=staffById[String(assignment.Staff_ID)]||{},brand=brandById[String(assignment.Brand_ID||a.Brand_ID)]||{},stage=dashboardPipeline_(a),work=(workByAffiliate[key]||[]).sort(function(x,y){return (dashboardTime_(x.dueAt)||Infinity)-(dashboardTime_(y.dueAt)||Infinity)})[0]||{},followup=(followupByAffiliate[key]||[]).sort(function(x,y){return (dashboardTime_(x.dueAt)||Infinity)-(dashboardTime_(y.dueAt)||Infinity)})[0]||{},event=lastEvent[key]||{},attention=Boolean(work.overdue||followup.overdue);pipeline[stage]++;if(!archived){activeAffiliates++;if(['NEW','ACTIVE'].indexOf(String(a.Prospect_Status))>=0)activeProspects++;if(stage==='CONNECTED'||stage==='TELEGRAM_CONNECTED')connected++;if(stage==='TELEGRAM_CONNECTED')telegramConnected++}affiliateItems.push({affiliateId:a.Affiliate_ID,affiliateUsername:a.Affiliate_Username||'',affiliateName:a.Affiliate_Name||'',brandName:brand.Brand_Name||'',brandCode:brand.Brand_Code||'',staffId:assignment.Staff_ID||'',staffName:actor.Display_Name||actor.Username||'',lifecycleStatus:a.Lifecycle_Status||'',prospectStatus:a.Prospect_Status||'',telegramStatus:a.Telegram_Status||'',priority:a.Priority||'',pipelineStage:stage,currentWorkId:work.workId||'',currentWorkType:work.type||'',workDueAt:work.dueAt||'',nextFollowupAt:followup.dueAt||'',lastInteractionAt:event.timestamp||'',lastOutcome:event.outcome||'',needsAttention:attention})});
  Object.keys(staffMetrics).forEach(function(id){var m=staffMetrics[id];m.connectionRate=m.activeAffiliates?Math.round(m.connectedAffiliates*1000/m.activeAffiliates)/10:0});var staffItems=Object.keys(staffMetrics).map(function(id){return staffMetrics[id]}).sort(function(a,b){return b.overdueWork+b.overdueFollowups-(a.overdueWork+a.overdueFollowups)||b.activeAffiliates-a.activeAffiliates||String(a.displayName).localeCompare(String(b.displayName))});var needsAttention=[];affiliateItems.forEach(function(a){if(a.currentWorkId&&dashboardTime_(a.workDueAt)<now)needsAttention.push({id:'WORK:'+a.currentWorkId,type:'OVERDUE_WORK',reason:'Overdue '+String(a.currentWorkType||'work').replace(/_/g,' ').toLowerCase(),affiliateId:a.affiliateId,affiliateUsername:a.affiliateUsername,staffName:a.staffName,dueAt:a.workDueAt});if(a.nextFollowupAt&&dashboardTime_(a.nextFollowupAt)<now)needsAttention.push({id:'FOLLOWUP:'+a.affiliateId,type:'OVERDUE_FOLLOWUP',reason:'Overdue follow-up',affiliateId:a.affiliateId,affiliateUsername:a.affiliateUsername,staffName:a.staffName,dueAt:a.nextFollowupAt})});needsAttention.sort(function(a,b){return dashboardTime_(a.dueAt)-dashboardTime_(b.dueAt)});var openWork=0,overdueWork=0,openFollowups=0,overdueFollowups=0,followupsDueToday=0;Object.keys(workByAffiliate).forEach(function(id){workByAffiliate[id].forEach(function(w){openWork++;if(w.overdue)overdueWork++})});Object.keys(followupByAffiliate).forEach(function(id){followupByAffiliate[id].forEach(function(f){openFollowups++;if(f.overdue)overdueFollowups++;if(dashboardToday_(dashboardTime_(f.dueAt),now))followupsDueToday++})});var contactsToday=staffItems.reduce(function(sum,s){return sum+s.contactsToday},0);return {generatedAt:new Date(now).toISOString(),kpis:{activeAffiliates:activeAffiliates,activeProspects:activeProspects,connectedAffiliates:connected,telegramConnected:telegramConnected,openWork:openWork,overdueWork:overdueWork,followupsDueToday:followupsDueToday,overdueFollowups:overdueFollowups,contactsToday:contactsToday,connectionRate:activeAffiliates?Math.round(connected*1000/activeAffiliates)/10:0},pipeline:pipeline,staff:staffItems,affiliates:affiliateItems.slice(0,1000),affiliateTotal:affiliateItems.length,needsAttention:needsAttention.slice(0,200)};
}
